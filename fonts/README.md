# Fonts Directory

This directory contains TrueType fonts for Vietnamese text rendering.

To enable Vietnamese characters with diacritics, place a TTF font file here:
- arial.ttf (or any TTF font that supports Vietnamese)
- DejaVuSans.ttf
- NotoSans-Regular.ttf

Without TTF fonts, the system will fallback to ASCII transliteration.